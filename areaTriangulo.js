// Leia a base e altura de um triângulo e calcule sua área (Área do triângulo: b * h / 2)

let Btriangulo = Number(prompt('Escreva a base do triângulo: '))
let Htriangulo = Number(prompt('Escreva a altura do triângulo: '))

let Atriangulo = (Btriangulo + Htriangulo) / 2

alert(Atriangulo)