// Faça um programa que determine o mostre os cinco primeiros múltiplos de 3, considerando números maiores que 0.

let inicio = 1
let fim = 5

for (let num = 3; num <= fim; num += 3) {
    console.log(`${num}`)
    
}